# NeonRoot repo index — lives at the root of a repo on cold storage.
# schema_version gates forward-compatible migration; revision is bumped on every
# commit and is the cheap first check for the "drive changed underneath you"
# conflict detection.
schema_version = 1
revision = 1
updated_at = "2026-07-13T00:00:00Z"

[[workspace]]
name = "python"
root = "workspaces/python"

[[workspace]]
name = "rust"
root = "workspaces/rust"
